# Surface & Slice

A private, dependency-free geometry explorer. Open `dist/index.html` to run it locally. Enter an explicit surface `z = f(x,y)`, choose a horizontal, vertical, or tilted plane, then compare the 3D intersection with the linked 2D view and set notation.

Run `node tests/smoke.cjs` for interaction and geometry checks.
